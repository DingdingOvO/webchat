apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: webchat

resources:
  - namespace.yaml
  - configmap.yaml
  - mysql.yaml
  - mongodb.yaml
  - redis.yaml
  - backend.yaml
  - frontend.yaml

commonLabels:
  project: webchat
  managed-by: kustomize
